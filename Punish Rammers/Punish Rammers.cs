/* First Limit
First Check Expression

Code: */
kill.Category == "VehicleAir"
/* Second Check

Code: */
// Save SoldierCollision Event
server.RoundData.setDouble("#" + victim.Name, player.TimeRound);
server.RoundData.setString("#" + victim.Name, killer.Name);